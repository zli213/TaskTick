export function SingleItems({
  content,
  id,
  due,
  project,
  label,
  description,
  priority,
  projectId,
}) {
  return (
    <div>
      <div>quan</div>
      <div>{content}</div>
      <div>menu</div>
    </div>
  );
}
