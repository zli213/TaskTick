export function SingleItems({
  title,
  _id,
  dueDate,
  project,
  label,
  description,
  priority,
  projectId,
  key
}) {
  return (
    <div>
      <div>quan</div>
      <div>{title}</div>
      <div>menu</div>
    </div>
  );
}
